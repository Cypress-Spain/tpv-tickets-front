
describe('DCSL Talk demo examples', () => {
  it('Hello world', () => {
    cy.visit('http://localhost:8080');
    cy.visit('/');
  });

  // it('Selectors', () => {});

  // it('Assertions', () => {});

  // it('Key events', () => {});

  // it('Use of fixture variables', () => {});

  // it('Server mocking', () => {});

  // it('Videos, commands & screenshots', () => {});
});
